/// question the user about vocabulary and return the percentage he had right
/// # Arguments
/// *`vocab`: the vocab to use for questioning
/// # Returns
/// returns the percentage the user had right
pub fn question_vocab(vocab: Vec<crate::dict::Vocab>) -> u8 {
    1u8 // placeholder
}
